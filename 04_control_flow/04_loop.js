/* --------------------------- Do While, While Loop --------------------------- */

// While Loop
// Condition define first
let index = 1;
while (index <= 10) {
    console.log(index);
    index++;
}

// Do-While Loop
// Always one time iteration run 
// after that check the condition
do {
    console.log(index);
    index++;
} while (index < 10);